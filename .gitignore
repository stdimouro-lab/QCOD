output/
node_modules/
web/node_modules/
web/dist/

# Local-only operational data and private source documents.
# These must never be committed — architectural PDFs, department maps, and
# anything generated from them (room candidates, section boundaries, backups)
# stay on this machine only.
private-source-documents/
imports/
data/generated/
data/backups/
data/private/
reports/generated/
